import React from 'react';
import {Grid} from "@mui/material";

export const Main = () => {
    return (
        <Grid xs={9} sx={{backgroundColor:'gray'}}>
            main
        </Grid>
    );
};

